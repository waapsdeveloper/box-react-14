import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'box-react-14',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
